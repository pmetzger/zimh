This directory contains legacy Visual Studio project files inherited from
SIMH.

They are retained for historical reference and possible one-off recovery
work, but they are no longer a supported primary build path for this
repository. The preferred Windows build path is the CMake-based workflow.

These files may be updated only minimally to keep paths understandable, and
they may be removed entirely in a future cleanup.
